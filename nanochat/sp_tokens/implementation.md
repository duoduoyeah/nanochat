
Init the tokenmap

-> we need to first create the tokenizer 
-> we need to save something in the tokenizer, so later could get the TokenMap

 * Create a script to load the base tokenizer and model weights to extract the lm_head embedding tensor.
 * Implement hierarchical k-means clustering (k=4) on the embedding tensor to generate an ancestry map for all pure tokens.
 * Generate new special token strings for the group tokens based on the hierarchy, including a root MASK token.
 * Create a new tokenizer by adding the new special tokens and save it to a file.
 * Create a script to load the new tokenizer and hierarchy data to properly populate and save a TokenMap instance.
 * Update nanochat/sp_tokens/token_map.py to allow for loading the new tokenizer and token map.

---
 - Level 0: 4096 pure tokens
  - Level 1: 1024 groups
  - Level 2: 256 groups
  - Level 3: 64 groups
  - Level 4: 16 groups
  - Level 5: 4 groups (as before)
  - Insert two extra coarser levels above that:
      - Level 6: 4 groups where each Level‑6 group contains 2 of the Level‑5 groups.
      - Level 7: 4 groups where each Level‑7 group contains 3 of the Level‑5 groups (again overlapping).
  - Level 8: the single root group.

---

Currently, we need to create many special tokens for our original tokenizer
those speical tokens are "group token", which means they correspond to a group of those original tokens. For all those tokens that could be generated by the model(no mater if it is speicla token, as long as the lm_head has it), then we name such kind of could-be-generate-token as pure token.
So group token are the group corresponding of those pure token.

Our current group method is kmean method:
-> consine similarity
-> bottom-up, so first create one token that cover all pure token, we name it mask token, it should has a special string repre contain the "MASK"
-> 4 divide, so the group tokens has a hierarchy struct, the higher level, this group token contain more pure token, and 1 level upper means 4 times
-> we use the embedding of each token as the value to compute kmean, thus we need to first load it in

so, we need to create a new tokenizer based on original tokenizer, and also will use a tensor that contains the embedding of each token, also we need to make sure we need add new method so that the new tokenizer could be load into the token_map for later use, i know this is a huge task, so we first plan from higher level, use a top-down method to solve this problem.

---